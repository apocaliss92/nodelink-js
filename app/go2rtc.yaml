api:
  listen: ":11984"
  origin: "*"

rtsp:
  listen: ":18554"

webrtc:
  listen: ":18555"

ffmpeg:
  bin: ffmpeg
  mjpeg: -hide_banner -an -vf "scale=-1:720" -q:v 5 -f mjpeg -

streams:
  # No streams registered yet
